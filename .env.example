# =============================================================================
# OpenClaw AWS Deployment Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
# These values are used by Terraform via TF_VAR_* environment variables.
# =============================================================================

# Environment Name (IMPORTANT for multi-developer setups)
# --------------------------------------------------------
# All resources are named: {project_name}-{environment}-*
# Each developer should use a UNIQUE environment name to avoid conflicts.
#
# Examples:
#   TF_VAR_environment=dev      → openclaw-dev-instance
#   TF_VAR_environment=jay      → openclaw-jay-instance
#   TF_VAR_environment=staging  → openclaw-staging-instance
#
TF_VAR_environment=dev

# AWS Region
# ----------
# The region prefix for Bedrock inference profiles is auto-detected:
#   eu-* regions → eu.anthropic.claude-*
#   us-* regions → us.anthropic.claude-*
#   ap-* regions → apac.anthropic.claude-* (Claude 4.5 not available)
#
# TF_VAR_aws_region=eu-west-1

# Instance Configuration
# ----------------------
# TF_VAR_instance_type=t3a.medium    # ~$27/month
# TF_VAR_volume_size=30              # GB

# Bedrock Models
# --------------
# Just specify the model name - region prefix is added automatically.
# Claude 4.5 models are available in US and EU regions only.
# In other regions, Claude 3.5 Sonnet is used automatically.
#
# TF_VAR_bedrock_model_primary=anthropic.claude-sonnet-4-5-20250929-v1:0
# TF_VAR_bedrock_model_fallback=anthropic.claude-3-7-sonnet-20250219-v1:0

# SSH Access Restriction
# ----------------------
# Restrict SSH to your IP (recommended for production)
# TF_VAR_allowed_ssh_cidrs='["YOUR_IP/32"]'

# Project Name
# ------------
# TF_VAR_project_name=openclaw
